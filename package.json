{
  "name": "voicecore",
  "version": "0.1.0",
  "private": true,
  "description": "VoiceCore — speech I/O module for xAI Realtime (Electron/browser)",
  "type": "module",
  "main": "./src/index.js",
  "scripts": {
    "start": "npm start --prefix test-app",
    "dev": "npm run dev --prefix test-app",
    "open": "npm run open --prefix test-app"
  }
}
